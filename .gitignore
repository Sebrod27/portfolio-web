# Raw source screenshot with real client names (LoreFlorez, mental health data) — never commit.
# The privacy-safe blurred version (obsidian_loreflorez_final.jpg) is the one used on the site.
assets/obsidian_loreflorez.png
